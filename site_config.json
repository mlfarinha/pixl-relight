{
  "_comment": "Top-level site config. `assets_root` is the URL prefix the website fetches scene grids, source thumbnails, and comparison images from. For local development, point it at `./web_assets`. For deployment, point it at your Hugging Face dataset's `resolve/main` URL. Trailing slash is stripped.",

  "assets_root": "./web_assets"
}